#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n;
    cin >> n;
    vector<vector<char>> roads;
    for (int i = 0; i < n; i++) {
        int m;
        char ch;
        cin >> m;
        vector<char> road(m);
        cin >> ch;
        for(int )
    }
    return 0;
}